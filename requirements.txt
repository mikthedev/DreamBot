discord.py>=2.6.0,<3.0.0
python-dotenv>=1.0.0
PyNaCl>=1.5.0
davey>=0.1.0
# SoundCloud audio for /play; Bluesky media helpers (YouTube not used)
yt-dlp[default]>=2026.3.10
certifi>=2024.0.0
aiohttp>=3.9.0
Pillow>=10.0.0
edge-tts>=6.1.0
# discord-ext-voice-recv is installed best-effort in start.sh (git+https).
# Keeping it out of requirements.txt stops the host egg from exiting 1 when
# GitHub/Cloudflare blocks the pip fetch during boot.
